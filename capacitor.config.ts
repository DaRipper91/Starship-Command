import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daripper91.starshipcommand',
  appName: 'Starship Command',
  webDir: 'dist',
};

export default config;
